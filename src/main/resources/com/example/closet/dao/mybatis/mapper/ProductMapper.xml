<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper
	namespace="com.example.closet.dao.mybatis.mapper.ProductMapper">
	<cache />

	<insert id="insertAuctionProduct">
		INSERT INTO PRODUCT (PRODUCTID, CATEGORYID, NAME,
		DESCRIPTION, TYPE,
		STATUS, REGISTERDATE, PERIOD, SUPPID, COLOR, SIZE,
		PICTURE1, PICTURE2, PICTURE3, PICTURE4)
		VALUES (#{productId},
		#{categoryId}, #{name}, #{description}, #{type},
		#{status},
		#{registerDate}, #{period}, #{suppId}, #{color}, #{size},
		#{picture1},
		#{picture2}, #{picture3}, #{picture4})
		AND INSERT INTO AUCTION
		(PRODUCTID, STARTPRICE, USED)
		VALUES (#{productId}, #{startPrice},
		#{used}})
	</insert>

	<insert id="insertGroupBuyProduct">
		INSERT INTO PRODUCT (PRODUCTID, CATEGORYID, NAME,
		DESCRIPTION, TYPE,
		STATUS, REGISTERDATE, PERIOD, SUPPID, COLOR, SIZE,
		PICTURE1, PICTURE2, PICTURE3, PICTURE4)
		VALUES (#{productId},
		#{categoryId}, #{name}, #{description}, #{type},
		#{status},
		#{registerDate}, #{period}, #{suppId},
		#{color}, #{size}, #{picture1},
		#{picture2},
		#{picture3}, #{picture4})
		AND INSERT INTO AUCTION
		(PRODUCTID, PRICE, PEOPLENUM)
		VALUES (#{productId}, #{price},
		#{peapleNum}})
	</insert>

	<update id="updateProduct">
		UPDATE PRODUCT
		SET
		DESCRIPTION = #{description},
		PICTURE1 = #{picture1},
		PICTURE2 = #{picture2},
		PICTURE3 = #{picture3},
		PICTURE4 = #{picture4}
		WHERE
		PRODUCTID = #{productId}
	</update>

	<delete id="deleteProduct">
		DELETE FROM PRODUCT
		WHERE
		PRODUCTID = #{productId}
	</delete>

	<select id="getProductDetail" resultType="Product">
		SELECT
		PRODUCTID,
		NAME,
		DESCN as description,
		CATEGORY as categoryId
		FROM PRODUCT
		WHERE PRODUCTID
		= #{productId}
	</select>

	<select id="getProductList" resultType="Product">
		SELECT
		PRODUCTID,
		NAME,
		DESCN as description,
		CATEGORY as categoryId
		FROM
		PRODUCT
		WHERE STATUS = 0 <!-- 판매 중인 상품 -->
	</select>

	<!-- <select id="searchProductList" resultType="Product"> -->
	<!-- select -->
	<!-- PRODUCTID, -->
	<!-- NAME, -->
	<!-- DESCN as description, -->
	<!-- CATEGORY as categoryId -->
	<!-- from PRODUCT -->
	<!-- WHERE lower(name) like #{value} -->
	<!-- </select> -->

</mapper>